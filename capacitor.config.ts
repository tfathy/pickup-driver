import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'net.driver.pickupsa',
  appName: 'pickup-driver',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
